# Dependencies
node_modules/

# Build output — regenerated by `npm run build` (and on publish via prepublishOnly)
dist/

# Example pipeline artifacts — regenerated by `npm run example`
example/build/
example/public/
example/react-ai-helmet.manifest.json

# Logs, caches, OS / editor cruft
*.log
npm-debug.log*
.DS_Store
.idea/
*.tsbuildinfo
